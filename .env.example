# Your domain name, pointing to this server (needed for HTTPS, which the app requires)
DOMAIN=stock.inter-diesel.example

# Optional: WhatsApp Business Cloud API (see WHATSAPP.md). Leave empty to keep it off.
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_API_VERSION=v23.0
